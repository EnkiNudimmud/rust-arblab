# Python runtime and data science dependencies used by the app.
# NOTE: do NOT include Rust crates (pyo3, etc.) here -- those are built via cargo/maturin.
# Updated for Python 3.13 compatibility - pandas 2.1.4 does not work with Python 3.13
pandas>=2.2.0
matplotlib>=3.8.4
seaborn>=0.13.2
scikit-learn>=1.3.2
statsmodels>=0.14.2
requests
websocket-client>=1.6.0
yfinance>=0.2.0
ccxt>=4.0.0
fastapi
uvicorn
jupyter
pytest
streamlit>=1.22.0
plotly>=5.0.0
# NumPy 2.x is compatible with Python 3.13
numpy>=1.26.4
nbformat>=5.10.4
# maturin is a Python tool used to build/install Rust-based Python extensions
maturin>=1.10.0
# Additional dependencies for Python 3.13 compatibility
scipy>=1.11.0,<1.14.0
# gRPC for dual backend support
grpcio>=1.60.0
grpcio-tools>=1.60.0